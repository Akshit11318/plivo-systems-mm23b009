# Python virtual environment (huge, machine-specific)
env/

# Secrets - real API tokens live in these
.env
.mcp.json

# Personal/local tool config, not project source
.claude/
.agents/
skills-lock.json
commands.md

# Supabase local/CLI state (unrelated side project in this dir)
supabase/

# Python
__pycache__/
*.pyc

# OS
.DS_Store

# --- Flaky Network assignment ---
# Build artifacts (make -> ./sender ./receiver)
/sender
/receiver
*.o

# Per-run generated output, not source
relay_stats.json
playout_log.json

# Raw zip handout - contents already extracted into systems_handout/
systems_handout.zip

# Private engineering log (Haiku agent maintains; local reference only)
CHANGELOG.local.md

# Private animated protocol explainer, local reference only
proto.html

# Plivo-provided materials, not deliverables
Systems_assignment.pdf
